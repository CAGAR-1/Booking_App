import 'package:flutter/material.dart';

class SuccessfullLogin extends StatelessWidget {
  const SuccessfullLogin({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("LOGIN SUCCESSFULL"),
    );
  }
}
