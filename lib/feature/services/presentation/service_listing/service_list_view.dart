import 'package:flutter/material.dart';

class ServiceListViewBuilder extends StatelessWidget {
  const ServiceListViewBuilder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}